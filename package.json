{
  "name": "correlius-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.23.1",
    "npm": ">=10.8.1 <11"
  },
  "scripts": {
    "build": "npm run check && astro build && npm run verify:build",
    "check": "astro check",
    "dev": "astro dev",
    "preview": "astro preview",
    "test": "npm run test:scanner && npm run build",
    "test:scanner": "node scripts/verify-build.mjs --self-test",
    "verify:build": "node scripts/verify-build.mjs"
  },
  "dependencies": {
    "astro": "7.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3"
  }
}
