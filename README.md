# Correlius site template

Upload `index.html` and `styles.css` to your GitHub Pages repository. Keep your existing `CNAME` file unchanged.

Before publishing, replace each `href="#"` with your real Vimeo, Substack, and Fractured Atlas links. Update the public email address if needed.
